name: Treinamento

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    runs-on: ${{ matrix.os }}
    steps:
     - name: Task 1 Exemplo
       run: echo 'Task 1 Exeplo'
##################################################################################################
##################################################################################################
 

